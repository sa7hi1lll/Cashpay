// Firestore rules starter — publish from Firebase Console after reviewing.
// Do NOT use open read/write rules in production.
//
// Example starting point:
// rules_version = '2';
// service cloud.firestore {
//   match /databases/{database}/documents {
//     match /campaigns/{campaignId} {
//       allow read: if true;
//       allow create: if request.auth != null && request.resource.data.ownerId == request.auth.uid;
//       allow update, delete: if request.auth != null && resource.data.ownerId == request.auth.uid;
//     }
//   }
// }
//
// For production, tighten public reads to approved/active campaigns and
// use custom admin claims for moderation.
